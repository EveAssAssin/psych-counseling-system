-- ============================================
-- Migration 020：員工職稱標籤（人工維護）
-- ============================================
-- 新增 job_tags（店長/副店長/正職/新人，可多選，第一個為主要）。
-- 由人工於員工詳情頁設定；員工同步不會寫入此欄，故不會被覆蓋。
-- 僅新增欄位，不影響既有資料。
-- ============================================

ALTER TABLE employees
  ADD COLUMN IF NOT EXISTS job_tags TEXT[];

COMMENT ON COLUMN employees.job_tags IS '職稱標籤（人工維護，同步不覆蓋）：店長/副店長/正職/新人，第一個=主要';
